APP_NAME=device_systems
APP_VERSION=3.0.0
ADMIN_USER=admin
# Generar localmente: python -c "import secrets; print(secrets.token_urlsafe(48))"
# No compartir ni versionar el valor generado.
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
# Opcional: por defecto usa device_systems.db en la raíz absoluta del proyecto.
# DATABASE_URL=sqlite:///./device_systems.db
